# GitHub repo (owner/repo) to list .usdz files from
GITHUB_REPO=hamzashaebi/sharingAR

# Path inside the repo where .usdz files live
GITHUB_PATH=models

# Branch to use for raw file URLs
GITHUB_BRANCH=main

# Optional: GitHub token for higher API rate limits (no auth needed for public repos)
# GITHUB_TOKEN=ghp_...

# --- Cloudflare R2 (S3 API) ---
# When set, models are listed and served from your R2 bucket instead of GitHub.
# Get S3 API credentials: R2 dashboard → Manage R2 API Tokens → Create API token (Object Read & Write).
# Bucket layout: create folders private/ and public/ in the bucket; put .usdz in models/private/ and models/public/
# (or set CLOUD_STORAGE_PATH to match your prefix).
#
# CLOUD_STORAGE_BUCKET=your-r2-bucket-name
# CLOUD_STORAGE_ACCESS_KEY_ID=your_r2_access_key_id
# CLOUD_STORAGE_SECRET_ACCESS_KEY=your_r2_secret_access_key
# CLOUD_STORAGE_REGION=auto
# CLOUD_STORAGE_ENDPOINT=https://YOUR_ACCOUNT_ID.r2.cloudflarestorage.com
# CLOUD_STORAGE_PATH=models
