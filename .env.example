# GitHub repo (owner/repo) to list .usdz files from
GITHUB_REPO=hamzashaebi/sharingAR

# Path inside the repo where .usdz files live
GITHUB_PATH=models

# Branch to use for raw file URLs
GITHUB_BRANCH=main

# Optional: GitHub token for API rate limits and for Upload to GitHub (read: list; write: upload)
# For upload, use a fine-grained or classic token with contents: read and contents: write.
# GITHUB_TOKEN=ghp_...
