# Storage provider: "github" (default) or "s3"
# STORAGE_PROVIDER=github

# --- GitHub (when STORAGE_PROVIDER=github or unset) ---
# GitHub repo (owner/repo) to list .usdz files from
GITHUB_REPO=hamzashaebi/sharingAR

# Path inside the repo where .usdz files live
GITHUB_PATH=models

# Branch to use for raw file URLs
GITHUB_BRANCH=main

# Optional: GitHub token for higher API rate limits (no auth needed for public repos)
# GITHUB_TOKEN=ghp_...

# --- S3-compatible (when STORAGE_PROVIDER=s3) ---
# Use with AWS S3, Cloudflare R2, MinIO, etc.

# --- Cloudflare R2 (S3 API) ---
# Set STORAGE_PROVIDER=s3 and use R2 S3 API credentials.
# Get credentials: R2 dashboard → Manage R2 API Tokens → Create API token (Object Read & Write).
# Account ID is in the R2 dashboard sidebar; endpoint = https://<ACCOUNT_ID>.r2.cloudflarestorage.com
# Bucket layout: put .usdz files under <S3_PREFIX>/private/ and <S3_PREFIX>/public/.
# Example: S3_PREFIX=view-byhamza-xyz/models → view-byhamza-xyz/models/private/ and view-byhamza-xyz/models/public/
#
# STORAGE_PROVIDER=s3
# S3_BUCKET=your-r2-bucket-name
# S3_ACCESS_KEY_ID=your_r2_access_key_id
# S3_SECRET_ACCESS_KEY=your_r2_secret_access_key
# S3_REGION=auto
# S3_ENDPOINT=https://YOUR_ACCOUNT_ID.r2.cloudflarestorage.com
# S3_PREFIX=view-byhamza-xyz/models
